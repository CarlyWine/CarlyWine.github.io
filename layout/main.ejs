<div class="container-fluid AndvariFill">
    <% for (var i = 0; i < page.description.length; i++) { %>
        <div class="row">
            <<%= page.description[i].type %> class="w-100 text-center">
                <%= page.description[i].text %>
            </<%= page.description[i].type %>>
        </div>
    <% } %>
</div>
